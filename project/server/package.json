{
  "name": "@space-invaders/server",
  "private": true,
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "build": "bunx tsc -p ../tsconfig.json --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "express": "^5.2.1",
    "luxon": "^3.7.2"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/luxon": "^3.7.1"
  }
}
